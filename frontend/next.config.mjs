/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "ui-avatars.com",
      "lh3.googleusercontent.com",
      "res.cloudinary.com"
    ],
  },
};

export default nextConfig;
